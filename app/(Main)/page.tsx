'use client'
import React from 'react'
import Header from '@/components/Header'

type Props = {}

const page = (props: Props) => {
  return (
    <div>
      <Header/>
    </div>
  )
}

export default page